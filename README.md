# mdd - Cloud Monitoring custom MetricDescriptor delete tool

`mdd` is the tool to delete custom metrics descriptor from Google Cloud Monitoring.

## Usage

```bash
mdd --project <project-id> --metric <metric-name>
```
